{
  "name": "x407-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "echo 'Run: npx wrangler secret put OPENAI_API_KEY && npx wrangler secret put ELEVENLABS_API_KEY'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

{
  "name": "fth-x402-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "FTH x402 Cloudflare Worker edge gateway — route matching, 402 generation, proof forwarding",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250301.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}

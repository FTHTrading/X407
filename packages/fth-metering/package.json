{
  "name": "fth-metering",
  "version": "0.1.0",
  "description": "FTH x402 metering — OpenMeter integration for usage events, quotas, and AI cost control",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "~5.4.0",
    "rimraf": "^5.0.0"
  },
  "license": "MIT"
}
